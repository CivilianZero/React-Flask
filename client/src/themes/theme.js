import { createMuiTheme } from "@material-ui/core";

export const theme = createMuiTheme({
	typography: {
		fontFamily: '"Open Sans"'
	},
	palette: {
		primary: {
			main: "#3A8DFF"
		},
		// secondary: "#1f1f1f",
		// error: "#d8000c",
		// bgcolor: "#f6f6f6"
	}
});
